/* global describe, it */

describe("hull-sql", () => {
  it("should detect the connection settings are empty", () => {});
  it("should show query builder if connection settings are set", () => {});

  it("should allow previewing results of the query", () => {});
  it("should allow importing all data", () => {});
  it("should allow adding literal column to the query", () => {});
  it("should allow using `last_updated_at` param in the query", () => {});
  it("should run the query in the interval sync", () => {});
});
